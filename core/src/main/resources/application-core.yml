
spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/workspace/kakaopay/kakaopay
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        default_batch_fetch_size: 500
#        dialect: org.hibernate.dialect.MySQL57Dialect
